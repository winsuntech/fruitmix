import paths from './paths'

// A drive has an uuid (now let's use repo to manage drive configuration update)
class Drive {

}

const createDrive(conf, callback) {

  if (conf.URI === 'fruitmix') {
    let drvpath = paths.get('drives') 
    
  } 
}
